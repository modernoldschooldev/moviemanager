// Redux types
export interface MovieActorAssociationType {
  actorId: string;
  movieId: string;
}

export interface SelectBoxSliceType {
  availableId: string;
  movieId: string;
  selectedId: string;
}
